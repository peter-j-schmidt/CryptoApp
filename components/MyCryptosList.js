import { StatusBar } from 'expo-status-bar';
import React from 'react';
import { StyleSheet, Text, View, ScrollView } from 'react-native';

export default function MyCryptosList() {
    return (
        <View>
            <ScrollView>
                <Text>My Cryptos!</Text>
            </ScrollView>
        </View>
    );
}